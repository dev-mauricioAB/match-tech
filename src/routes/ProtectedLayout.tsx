// Re-exports RootLayout as the named Component for React Router v7 lazy() routes.
// RootLayout renders the nav, background, Outlet, and logout modal — the full authenticated shell.
export { default as Component } from "@/layouts/RootLayout";
